
# dev

orb -m nixos nix-shell -p nodejs python3
source .venv/bin/activate
python3 manage.py runserver

# setup

python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt -r requirements.dev.txt
npm i